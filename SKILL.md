---
name: book-compress
description: 把一本书（PDF/EPUB/TXT/DOCX，多为创业类工具书）压缩成保留核心观点与例子的精读版，输出 Kindle 可读的 EPUB + Markdown 源稿 + 压缩报告。当用户要求压缩书籍、提炼书的精华、做书的精读版/摘要时使用。
---

# 书籍压缩 Agent（book-compress）

把一本书压缩成"该有的观点一个不少、废话一字不留"的精读版。你（Agent）就是执行者：按本文件的流程和原则操作，不要即兴发挥。

## 输入

`/book-compress <书的路径> [要求]`，例如：

```
/book-compress ~/books/精益创业.pdf
/book-compress ./lean-startup.epub 我只有半小时，案例可以少留
```

`[要求]` 可以覆盖默认行为（如指定保留/丢弃偏好、目标篇幅），但不能违反下面的最高原则。

## 最高原则（任何情况下不可违反）

1. **压缩比由内容决定，不预设时长。** 不用固定字数倒推。逐章只删纯冗余，删完剩下的就是保真下限。压缩比是结果，不是目标。用户要求"更快读完"时，只能通过丢弃低价值章节和压缩例子实现，且必须在报告中说明丢了什么。
2. **严格按原书章节，防内容偏移。** 压缩版目录 = 原书目录，一章对一章，不合并、不重排、不跨章搬运内容。每章内部保持原文论证顺序（观点→解释→例子的展开次序不变）。
3. **两层保真（讲书模式的核心）。** 成品不是笔记，是把每一章"讲给人听"的连贯文章。
   - **事实层（锁死）**：数字、人名、机构、实验设计、结果、观点归属、"可能/往往"等限定词——必须与原文一致，可逐条回源校验。禁止：改数字、换人名、把"可能"说成"一定"、给作者安上他没说的主张。
   - **叙述层（放开）**：读完一章后用自己的话把这一章讲出来——像向没读过的人转述：作者先抛出什么现象/问题，提出什么解释，用什么实验或案例证明，实验怎么设计的，结论的边界在哪，怎么防范。逻辑关系（递进、转折、对比、因果）必须讲明白，这是成品可读性的生命线。
   - 核心观点的定义句优先引用原句（一章至少 1~3 处原文引句，标注位置），其余用自己的话讲。依据：SummaC/FactCC 表明纯改写最易失真，故只放开叙述、锁死事实；关键句引用让读者始终锚定在原书上。
4. **禁增原则。** 讲述中出现的事实断言必须源自本章原文；你自己的话只做转述和串联，不引入原文没有的新事实、新数字、新案例。宁可删不可改：拿不准的内容整段不讲，并在报告说明。
5. **限定词保真。** "可能""在 X 情况下""早期""某些"等程度词、条件、范围必须原样保留，禁止绝对化。这是最常见的隐性失真。
6. **全书去重。** 创业书常用"一个观点翻来覆去讲"凑篇幅。同一观点重复出现时，只在其首次出现且论述最完整的章节完整保留，其余出现处在报告中标注"该观点另见第 X 章"。跨章去重必须基于实际比对，不得凭印象合并相似但不同的观点。

## 压缩中只删这四类（其余内容默认保留）

- 重复论证：同一观点的第二次及以后的展开（去重规则见上）
- 寒暄与元话语：开场白、章节间过渡口水话、"在上一章我们讲到"、作者自述写作缘由（除非本身是核心内容）
- 营销话术：夸本书重要性的段落、名人背书铺陈
- 跑题展开：与章节核心论点无关的支线内容

**默认保留**：核心观点及其解释、支撑案例（尤其含具体数字/人名/结果的）、方法步骤、清单、金句、反例与警告。

## 执行流程

工作目录约定：当前工作区根目录下 `.book_work/`（中间产物）与 `output/`（成品）。脚本随 skill 放在 `scripts/` 中；先确定本文件所在的 skill 目录，再使用当前工作区可用的 Python 解释器运行脚本。若依赖缺失，按同目录的 `INSTALL.md` 安装。不要假设调用者的项目里存在 `tools/`。

### Step 0 提取

```bash
<python> <skill-dir>/scripts/extract_book.py <书的路径>
```

产出 `.book_work/<书名>/fulltext.md`（带章节标记）与 `meta.json`。若 stderr 出现扫描版警告，停下来告知用户需要先 OCR，不要继续。

### Step 1 书型预判（只做预期，不做删减目标）

读 meta.json 的章节列表 + 抽样 fulltext.md（每章首尾各 300 字），判定书型并记录预期压缩区间：

| 书型 | 特征 | 预期压缩到 |
|---|---|---|
| 方法论/框架型 | 核心框架+多章展开（如《精益创业》） | ~15% |
| 观点/论证型 | 少数核心论点+大量论证（如《从0到1》） | ~10% |
| 案例/叙事型 | 主线故事+嵌套教训 | ~20% |
| 工具/清单型 | 步骤、checklist、手册密集 | ~30% |

区间仅用于给用户预期和最终报告对照；每章实际压缩量按"删完冗余后剩多少"定。

### Step 2 逐章讲书（Map）

对每一章依次处理（章数多时可用子代理并行，但并行时跳过 Step 2.3，由汇总阶段统一去重）：

**2.1** 从 fulltext.md 按章节标记切片读入本章全文，**完整读一遍再动笔**——先在脑里回答三个问题：这一章要解决读者什么问题？作者是分哪几步论证的？哪些实验/案例是支柱证据？

**2.2** 写本章讲书稿，写入 `.book_work/<书名>/chapters/ch{N}.md`。**成品必须是连贯的文章，不是观点清单**：

**篇幅规则（不预设字数）：** 动笔前先盘点本章的论证单元——每个论点、每个实验/案例、每份清单各算一个——列成单元清单，逐个判断"讲/略讲/不讲"（不讲的唯一正当理由是冗余或跑题，必须写明）。然后把要讲的单元全部讲清，讲完停笔。**字数是覆盖这些单元的结果，不是输入**；禁止先定字数再砍内容。写完后若总量明显偏大，压缩的是叙述密度（同样的事实用更少的句子讲），而不是删论证单元。

```markdown
# 第N章 <标题>

<!-- meta: 原文字数 X | 讲稿字数 Y | 引用小节: ... -->

（一篇连贯的讲解文章，按原书小节顺序推进。写法要求：

1. 开篇 2~3 句：这一章在整个书里的位置，要回答读者什么问题。
2. 主体：按作者的实际论证顺序把这一章"讲"出来。每个论点：先用白话说明作者的主张是什么，
   再引用最关键的原句（每章 1~3 处，标注小节）；实验/案例作为证据自然嵌进叙述——
   谁做的、怎么做的、数字结果、说明了什么，一样都不能少，但要用讲故事的语气串起来。
3. 论证关系必须显式交代："作者先立了一个现象 → 指出常见的解释站不住脚 → 给出自己的机制 →
   用对照实验排除竞争解释 → 最后提醒边界条件"。转折、递进、例外一个不落。
4. 作者给的防范建议/行动清单单独收一段，逐条保留。
5. 结尾一段收束：这一章带走什么。
）
```

硬性禁止（讲书稿里出现即判失败）：
- 用 bullet 清单代替叙述（防范清单和步骤清单除外——清单类内容本来就是清单）；
- 只列观点不给"为什么""怎么证明的"；
- 例子没有数字/结果，只有"效果显著""大幅提升"这类模糊词；
- 引入原文没有的事实。

**2.3** 每章完成后更新 `.book_work/<书名>/viewpoints.md`（全书观点清单）：观点原句、出现章节、论述最完整的位置。后续章节讲书前先读它——这是 BooookScore 指出的"后章不知前章"失败模式的解法，也是去重依据。讲后章时：与前章重复的观点不再展开，用一句话带过并回指前章。

**2.4** 本章偏移校验（不过则重写本章）：
- 讲稿中每个事实断言（数字/人名/实验设计/结果/归属）回 fulltext.md 定位，定位不到 → 失败；
- 检查限定词是否被删/被绝对化 → 失败；
- 核心定义句是否为原句引用 → 缺失 → 失败；
- 论证顺序是否与原章一致、逻辑关系是否交代清楚 → 顺序错乱或只有结论没有论证 → 失败。

**2.5** 写**本章重点总结**，附在本章讲书稿末尾（替代任何形式的目录式导图）。方法论依据：Brown & Day 总结宏观规则（1983）+ 商业 executive summary 的 BLUF 惯例 + 教科书 Key Takeaways 对齐原则。

- **定位**：总结为记忆服务，不为检索服务。讲稿负责"讲透"，总结只回答"读完这章，该记住什么、做什么"；假设读者只读总结也能抓住这一章。
- **结论先行（BLUF）**：先用一小段话（一两句）给出本章核心主张——本章如果只能记住一句话，就是它。
- **条数由内容决定，不由配额决定**：动笔前先问"这一章真正立了几个独立主张"，有几个收几个；通常落在 3~5 条（行业共识区间），但这是结果不是目标。若超过 7 条，说明"主张"的定义松了——多数是同一主张的子情形，应做上位化归并（Brown & Day 规则3：用一个上层概念代替一串列举）。
- **操作顺序（Brown & Day 宏观规则）**：① 删琐碎（与本章论证无关）；② 删冗余（重复论证）；③ 上位化归并子情形；④ **优先选原句主题句**（讲稿中已审计的引句优先）；⑤ 只有文中没有现成主题句时才自创归纳，**自创句必须逐条回源核对**（该规则最容易失真）。
- **只收判断，不收证据**：实验、案例、数字一律不进总结（删除测试：删掉这条，读者照书行动会缺一块 → 收；只是"本章讲过" → 不收）。
- **对齐原则**：每条总结对应本章的一个论证单元（即 2.2 盘点出的单元清单中的【讲】级单元的结论面），不对应单元的条目删掉。
- **保真**：限定词（可能/往往）保留；不得新增观点；纯综合、无新信息。
- **格式统一**：全书每章同一结构（核心主张一段 + 编号要点列表），一条一两句，可扫读。
- **不做累积**：每章只总结本章；全书全局由书前"核心观点清单"与书末"如果只做三件事"承担。
- 格式：

```markdown
## 本章总结

（一两句：本章核心主张，BLUF）

1. （每条对应一个独立主张，一条一两句）
2. ...
```

### Step 3 汇总（Reduce）

按原书章节顺序把各章讲书稿合成 `output/<书名>/压缩版.md`，各章末尾的"本章总结"随章节自然带入（不合并、不重排）。全书结构：

```markdown
# 《书名》精读版

> 原书 X 万字 → 精读版 Y 字（压缩至 Z%）。观点与例子均摘自原书，每条可溯源。

## 全书导读
（一段话：这本书解决什么问题、核心主张是什么——只允许复述原书引言/结论中的原意）

## 核心观点清单
1. "<原句>"（第N章，另见第M章）
...

## 第N章 <标题>
（各章讲书稿正文，按原书章节顺序直接拼接——成品主体是连贯的讲解文章；
每章末尾自带"本章总结"，随章节自然带入）

## 关键案例索引
（全书重要案例一览：案例名 → 所在章 → 支撑的观点）

## 如果只做三件事
（从原书的行动建议中挑三条最可执行的，注明出处章节）
```

### Step 3.5 讲稿语义审计（讲书模式必做，至少抽 2~3 章；用户要求全量则全量）

讲书稿是代理"自己理解后转述"的，叙述层失真机检抓不到，必须由**另一个未参与写作的代理**做逐节对照审计：

**审计对象**：原章全文（fulltext.md 对应切片）vs 本章讲书稿。
**审计代理要求**：逐节并排比对，把讲稿的每个事实性断言/因果断言回原文验证；同时反向检查——原文的每个论证单元在讲稿中是否有对应（清单标【讲】但正文缺失 = 失败）。
**五类失真分级**：
- **A 事实错误**：数字/人名/实验设计/结果/归属与原文不符 → 必须修复；
- **B 语义漂移**：相关被讲成因果、限定词被弱化或绝对化、过度概括、"作者认为X"实际原书是"有人认为X" → 必须修复；
- **C 无据新增**：讲稿含原文没有的断言（含外部知识补充） → 必须删除或回源改写；
- **D 关键遗漏**：漏掉改变结论强度的限定条件/边界/反例 → 必须补；
- **E 风格差异**：详略取舍、语言组织不同但事实无损 → 记录不修。
**输出**：差异清单（每条含：讲稿原句 / 原文原句 / 分类 / 建议修法），写入 `.book_work/<书名>/audit/ch{N}_audit.md`。
**修复回路**：A/B/C/D 由写章代理（或主代理）修复后，审计代理复检该章，直至 A/B/C/D 清零。审计结果汇总进压缩报告（含各类问题数量），不粉饰。

### Step 4 全书校验与报告

**4.1** 校验：原书每章在压缩版中有一一对应；每章至少保留 1 条观点或例子（除非整章被判为纯冗余，须在报告说明）；逐条观点回源定位通过；语义审计 A/B/C/D 清零（未审计章节须在报告中注明"未做语义审计"）；本章总结专项——每章末尾都有总结、条数与本章独立主张数一致（3~5 为常态而非配额）、自创归纳句已回源、内容可溯源（缺总结或无对齐 → 修）。

**4.2** 生成 `output/<书名>/压缩报告.md`：
- 书型判定与依据、预期压缩区间 vs 实际压缩比
- 各章压缩明细表（原字数→压后字数→删了什么类别）
- 观点溯源对照表（观点 → 原文位置）
- 丢弃内容说明 + 跨章去重记录

**4.3** 生成封面（默认程序生成，保证任何书都有封面）：

```bash
<python> <skill-dir>/scripts/make_cover.py --title "<书名>" --author "<原书作者>" \
  --label "精读版" --out output/<书名>/cover.jpg
```

- 默认**不传 --source**：程序生成 1200x1600 排版封面（深色底+书名+作者+角标）。
- 仅当用户想要原书封面时才加 `--source <书的路径>`（从源 EPUB 提取）。
- 用户自带封面（如 Canva 导出的 1600x2560 JPG）直接用它当 cover.jpg。

生成 EPUB：

```bash
<python> <skill-dir>/scripts/build_epub.py output/<书名>/压缩版.md \
  -o output/<书名>/压缩版.epub --title "《书名》精读版" --author "<原书作者>" \
  --cover output/<书名>/cover.jpg
```

`build_epub.py` 还需要系统命令 `pandoc`；缺失时按 `INSTALL.md` 安装，不要静默跳过 EPUB 交付。

### Step 5 交付

向用户报告：书型与压缩比、成品三个文件的路径、核心观点条数、去重与丢弃概况、每章末尾"本章总结"的存在、建议抽查方式（对照压缩报告里的溯源表随机核对几条）。

## 边界情况

- **扫描版 PDF**：提示用户先 OCR（ocrmypdf），不勉强处理。
- **章节识别失败**（全文本挤成一章）：告知用户，改用人工指定章节标记或换格式。
- **用户指定篇幅**：按最高原则 1 处理，只删低价值内容并在报告说明。
- **非虚构工具书之外的书**（小说等）：明确告知本流程为非虚构工具书设计，问用户是否继续。

## 设计依据（内部参考，不向用户长篇输出）

- BooookScore (arXiv 2310.00785)：分层压缩连贯性优于迭代精炼，遗漏错误率约低一半；逐章传入观点清单是"后章不知前章"的解法。
- SummaC (TACL 2022) / FactCC (EMNLP 2020)：逐句回源校验；改写越重校验越漏检 → 摘录优先。
- getAbstract/Blinkist：固定模板 + 独立复审 = 质量核心；压缩报告扮演复审角色。
